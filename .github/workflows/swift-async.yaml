# Generated file. DO NOT EDIT.
'on':
  push:
    paths:
    - swift-async/**
    - .github/workflows/swift-async.yaml
  pull_request:
    paths:
    - swift-async/**
    - .github/workflows/swift-async.yaml
  workflow_run:
    workflows:
    - scenario-server
    types:
    - completed
jobs:
  test:
    runs-on: macos-14
    if: ${{ github.event.workflow_run == null || github.event.workflow_run.conclusion == 'success' }}
    steps:
    - uses: actions/checkout@v4
    - run: |
        mkdir -p ~/Library/Containers/com.docker.docker/Data
        curl https://desktop.docker.com/mac/main/arm64/Docker.dmg --output Docker.dmg
        sudo hdiutil attach Docker.dmg
        sudo /Volumes/Docker/Docker.app/Contents/MacOS/install --accept-license --user=runner
        sudo hdiutil detach /Volumes/Docker
        /Applications/Docker.app/Contents/MacOS/com.docker.backend -accept-license -unattended &
        sleep 60
        ls /var/run/*.sock
        docker ps
    - run: swift test -c release
    defaults:
      run:
        working-directory: swift-async
